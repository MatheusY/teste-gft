server:
  port: 8090

spring:
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://postgresql-db:5432/teste_gft
    username: postgres
    password: postgres
    
data:
  path: /app/massa/